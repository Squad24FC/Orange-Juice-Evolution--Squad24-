<h1>Aula</h1> <%= link_to 'Criar aula', new_lesson_path %>

<p>Essa é a página que vai conter todas as aulas</p>

<ul>
  <% @lessons.each do |lesson|  %>
  <li><%= lesson.name %> | <%=  link_to 'Editar', edit_lesson_path(lesson) %> | <%= link_to 'Remover', lesson_path(lesson), method: :delete, data: {confirm: 'Are you sure?'} %></li>
  <% end  %>
</ul>