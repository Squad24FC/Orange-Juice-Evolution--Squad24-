// lib/controllers/show_lesson_detail_card_controller.js
import { Controller } from "@hotwired/stimulus"

export default class extends Controller {
  connect() {
    console.log("The 'show_card' controller is now loaded!")
  }
}
